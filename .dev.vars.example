# 仅用于本地 wrangler dev。复制为 .dev.vars，绝对不要提交真实值。
SETUP_TOKEN=replace-with-a-random-value-at-least-32-characters

# 可选：后台“从 Cloudflare 同步”所需的只读 API Token。
# 最小权限：Zone Read，以及读取 Email Sending 域名所需权限。
CF_API_TOKEN=
